services:
  - type: web
    name: gpt-assistant-bot-v2  # <–– новое имя
    env: python
    build:
      pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py